This project is not meant to be linked to. Instead, different projects that need these non-core standalone 
utilities should just link the source files, so that the code is embedded inside that other projects.
To further enforce this restriction, this project does not build in Release configuration.

Theoritcally, there should be no public types here.